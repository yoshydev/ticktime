#!/bin/sh
# ステージ済みの変更を gitleaks でスキャンし、機密情報を検出したらコミットを中止する

if ! command -v gitleaks >/dev/null 2>&1; then
	echo "pre-commit: gitleaks が見つかりません。インストールするか、--no-verify でスキップしてください。" >&2
	exit 1
fi

gitleaks git --pre-commit --staged --redact --verbose
